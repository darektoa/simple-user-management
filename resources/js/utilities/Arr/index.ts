import isEmpty from './isEmpty';
import isIndexPair from './isIndexPair';
import isKeyPair from './isKeyPair';
import isNotEmpty from './isNotEmpty';
import isNull from './isNull';
import merge from './merge';
import softMerge from './softMerge';

const Arr = {
    isEmpty,
    isIndexPair,
    isKeyPair,
    isNotEmpty,
    isNull,
    merge,
    softMerge,
};

export default Arr;
